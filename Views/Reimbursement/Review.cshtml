@model SimplexLawFirm.Models.ReimbursementClaim
@{ ViewData["Title"]="Review "+Model.ClaimNumber; Layout="_AdminLayout"; }
<div class="breadcrumb-line"><a href="/Reimbursement/DirectorQueue">Reimbursement approvals</a><span>/</span><span>@Model.ClaimNumber</span></div>
<div class="page-hero compact"><div><span class="eyebrow">Director decision required</span><h1>R @Model.Amount.ToString("N2") · @Model.ExpenseType</h1><p>@Model.Attorney.FullName · @Model.Case.CaseNumber · @Model.ExpenseDate.ToString("dd MMM yyyy")</p></div></div>
<div class="workspace-grid"><section class="surface"><h2>Evidence and policy</h2><p>@Model.Description</p><p><b>Matched:</b> @Model.MatchedActivityType #@Model.MatchedActivityId</p><p><b>Policy limit:</b> R @Model.PolicyLimitSnapshot.ToString("N2")</p><p><b>Delegated limit:</b> R @Model.DelegatedLimitSnapshot.ToString("N2")</p><p><b>Classification:</b> @Model.Classification</p><p class="muted">@Model.ClassificationReason</p><a class="btn-secondary-modern" href="/Reimbursement/DownloadProof/@Model.Id">Open proof</a></section><section class="surface"><h2>Record decision</h2><form method="post" action="/Reimbursement/Decide" class="stack-form">@Html.AntiForgeryToken()<input type="hidden" name="ClaimId" value="@Model.Id"/><label>Reason<textarea name="Reason" minlength="5" rows="5" required></textarea></label><div class="estimate-actions"><button class="btn-primary-modern" name="Approve" value="true">Approve claim</button><button class="btn-secondary-modern" name="Approve" value="false">Refuse claim</button></div></form></section></div>
